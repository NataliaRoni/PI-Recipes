import React from "react";
import ReactDOM from "react-dom";
import "./index.css";
import App from "./App";
import reportWebVitals from "./reportWebVitals";
import { Provider } from "react-redux";
import store from "./store/store";

// Renderiza el contenido principal de la app en el elemento con el id "root":
ReactDOM.render(
  // Provider permite a los componentes de la app acceder al estado de Redux.
  // StrictMode activa comprobaciones y advertencias adicionales para detectar problemas en la app.
  <Provider store={store}>
    <React.StrictMode>
      <App />
    </React.StrictMode>
  </Provider>,
  document.getElementById("root")
);

// If you want to start measuring performance in your app, pass a function
// to log results (for example: reportWebVitals(console.log))
// or send to an analytics endpoint. Learn more: https://bit.ly/CRA-vitals
reportWebVitals();
